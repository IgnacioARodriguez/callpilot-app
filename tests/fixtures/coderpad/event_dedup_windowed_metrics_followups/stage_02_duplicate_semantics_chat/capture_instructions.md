This screenshot may be an exact copy of stage 01 if the visible editor did not change.

If recaptured, show the same `count_event_types` implementation and a sample where the same id appears later with another type.
