1. Pegá `code.py`.
2. Ejecutalo.
3. Debe aparecer un traceback, normalmente `KeyError: 'id'` o `ValueError` al convertir `high`.
4. Capturá helpers, función principal, input sucio y traceback.
5. No corrijas el parser antes de capturar.
