# Stage 00 Capture

Open the instructions or README surface only.

Visible text should include:

```text
Implement format_user(name).
Return a display_name string.
Trim leading and trailing whitespace.
Collapse internal whitespace to a single space.
Use title case, not uppercase.
Empty or whitespace-only input returns "Anonymous".
```

Save the screenshot as `instructions.png`.
