# Stage 01 Capture

Create two screenshots from the same checkpoint:

1. `instructions.png`: show the README/instructions contract from stage 00.
2. `implementation.png`: show `src/formatter.py` with:

```python
def format_user(name):
    return name


print(format_user(" ada lovelace "))
```
