# LeetCode Screenshot Scenario

Active mode: `live_coding`

Input context:

```text
Two Sum
Example 1:
Input: nums = [2,7,11,15], target = 9
Output: [0,1]
Constraints:
2 <= nums.length <= 10^4
```

Expected prompt sections:

- active_mode
- output_format
- coding_language_preference
- screen_context
- user_input

Expected answer shape:

- Problem detected
- Approach
- Solution
- Complexity
- Edge cases
- What to say out loud

Known limitations:

- V0 uses pasted visible text, not OCR, until desktop screenshot/vision is upgraded.
