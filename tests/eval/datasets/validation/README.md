# Validation Datasets

Validation source media and manifests must live outside the repository and be
referenced through `CALLPILOT_EVAL_VALIDATION_DIR`.

This directory is only a placeholder for schema/docs.
