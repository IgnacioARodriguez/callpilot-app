# Holdout Datasets

Holdout source media and manifests must live outside the repository and be
referenced through `CALLPILOT_EVAL_HOLDOUT_DIR`.

Do not inspect holdout raw materials while implementing fixes.
